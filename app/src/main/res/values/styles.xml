<resources>

    <!-- For the uptime clock -->
    <attr name="clock" format="reference"/>

    <!-- Base Theme -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Light Theme -->
    <style name="AppTheme.Light" parent="AppTheme.Base">
        <item name="android:textColorPrimary">@android:color/primary_text_light</item>
        <item name="android:textColorSecondary">@android:color/tertiary_text_light</item>
        <item name="android:background">@android:color/background_light</item>
        <item name="android:textColorAlertDialogListItem">?android:textColorSecondary</item>
        <item name="clock">@drawable/ic_clock_black_24dp</item>
    </style>

    <!-- Dark Theme -->
    <style name="AppTheme.Dark" parent="AppTheme.Base">
        <item name="android:textColorPrimary">@android:color/primary_text_dark</item>
        <item name="android:textColorSecondary">@android:color/secondary_text_dark</item>
        <item name="android:background">@android:color/background_dark</item>
        <item name="android:textColorAlertDialogListItem">?android:textColorPrimary</item>
        <item name="clock">@drawable/ic_clock_white_24dp</item>
    </style>

    <!-- Tab style -->
    <style name="TabStyle" parent="Widget.Design.TabLayout">
        <item name="android:background">?android:colorPrimary</item>
        <item name="tabIndicatorColor">@android:color/primary_text_dark</item>
        <item name="tabSelectedTextColor">@android:color/primary_text_dark</item>
        <item name="tabTextAppearance">@style/TabTextAppearance</item>
    </style>

    <!-- Text appearance for tab names -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/colorLight</item>
    </style>

</resources>
